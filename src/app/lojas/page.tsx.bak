"use client";

import Link from "next/link";
import StoreCard from "@/components/StoreCard";

export default function LojasPage() {
  const stores = [
    {
      id: "dkworks",
      store_name: "DKWorks Studio",
      description: "Estúdio de desenvolvimento e design - DKWorks",
      logo: "/img/logos/dkTransparente.png",
      external_url: "https://dkworksstudio.base44.app/"
    },
    {
      id: "vitrine-segura",
      store_name: "Vitrine Segura",
      description: "Loja de vitrines online - Vitrine Segura",
      logo: "/img/logos/vitrineSegura.png",
      external_url: "https://vitrine-segura.vercel.app/"
    }
  ];

  return (
    <div className="min-h-screen bg-gray-50 py-8">
      <div className="max-w-6xl mx-auto px-4">
        <div className="text-center mb-8">
          <h1 className="text-4xl font-bold text-[#003049] mb-4">Lojas de Modelo-SC</h1>
          <p className="text-lg text-gray-600 max-w-2xl mx-auto">
            Diretório de lojas — clique em "Visitar Loja" para abrir a landing externa.
          </p>
        </div>

        <div className="bg-white rounded-lg shadow-lg p-8 text-center max-w-2xl mx-auto mb-8">
          <h2 className="text-2xl font-semibold text-[#003049] mb-2">Diretório de Lojas</h2>
          <p className="text-gray-600">
            Lojas adicionadas manualmente pela equipe. Caso queira alterar a logo, coloque o arquivo em <strong>/public/img/logos/</strong>.
          </p>
          <div className="mt-6">
            <Link href="/" className="inline-block bg-[#003049] hover:bg-[#002038] text-white px-6 py-3 rounded-lg font-semibold transition">
               Voltar ao Início
            </Link>
          </div>
        </div>

        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
          {stores.map((s) => (
            <StoreCard key={s.id} store={s} />
          ))}
        </div>
      </div>
    </div>
  );
}
