/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      // Supabase storage public host (replace with your project host if different)
      "poltjzvbrngbkyhnuodw.supabase.co",
    ],
  },
};

export default nextConfig;
